# 本地开发补丁：dsh checkout 内 `dsh --patch <本文件>` 或 dev:web overlay 使用。
# 注意：name 是本机绝对路径，不要提交到远端共享环境。
- insert:
    - id: dsh-fim
      name: file:///C:/Users/DJ028191/pyai-meta-repo/dsh-sparrow/plugins/dsh-fim/src/index.ts
      config:
        baseURL: https://api.deepseek.com/beta
        model: deepseek-chat
        maxTokens: 64
        apiKeyEnv: DEEPSEEK_API_KEY
        requestTimeoutMs: 30000
        maxBodyBytes: 65536
        maxPromptChars: 32768
        triggerPauseMs: 400
