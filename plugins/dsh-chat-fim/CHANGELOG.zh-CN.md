# 更新日志

简体中文 | [English](CHANGELOG.md)

## 0.1.5-rc.1（2026-09-10）

- 版本线对齐官方 dsh 0.1.5-rc.1。
- 在 dsh 0.1.5 上，联想开关与候选菜单恢复显示：上一版因不认识新的会话格式而把它们整体隐藏了。
- 每次请求联想不再拷贝整份会话日志，长会话下响应更稳。

## 0.1.2-rc.1.1（2026-09-09）

- 联想转动环不再出现在别的会话上：只有真正在联想的那次请求所属的会话才画环。
- dsh 升级到本插件尚不支持的版本时，插件会自动停用（dsh 与其它插件不受影响），不再带病运行。
- 联想转动环在弹窗打开时随背景一起虚化（此前浮在遮罩之上、盖住弹窗）。
- 本插件不认识的会话存储格式不再请求续写（不发起计费上游调用）。

## 0.1.2-rc.1（2026-09-05）

- 版本对齐官方 dsh 0.1.2-rc.1（稳定线）。
- FIM 开关随模型选择即时追平：切换模型立即更新支持状态，两个不支持模型之间切换不再闪出卡片。
- 点击建议卡的非按钮区域不再抢走输入框焦点（Tab 采纳保持可用）。
- 拒绝跨站调用补全端点（该路由以服务端 API key 执行计费上游请求）。
- 输入框含 @ 元素（文件/提示词）时 Tab 采纳恢复正常：采纳区间按 token 坐标映射，不再被展开后的文本坐标越界拒绝。
- 联想卡左下角新增 🐦 dsh-sparrow 品牌行。

## 0.1.0 (2026-09-02)

- 转正 0.1.0（与 0.1.0-alpha.3 一致）。

## 0.1.0-alpha.3 (2026-09-02 · pre-release)

- 修复联想光环与输入卡片外框之间的缝隙（光环元素缺 box-sizing）。
- README 截图改为绝对 URL 引用，图片不再打包进 npm 包。

## 0.1.0-alpha.2 (2026-09-01 · pre-release)

- 版本对齐合集 0.1.0-alpha.2（功能与上版一致）。

## 0.1.0-alpha.1 (2026-09-01 · pre-release)

- 首个发布版本，先发 `next` 渠道供 owner 验证，稳定版 `0.1.0` 待转正
- 输入框续写联想：停顿后触发，浮层卡片样式对齐官方 @ 候选菜单；**Tab** 采纳、**Esc** 关闭（点击亦可）；官方 @/斜杠菜单打开时让位
- 上游：DeepSeek **FIM 补全（Beta）**（`/beta/completions` + 说话人转写提示词）；补全模型**跟随主模型**（自动：v4-pro / v4-flash，vision 等场景回退 pro），卡片角落显示实际模型与温度
- 开关文案中英统一为 **FIM**（2026-09-01 拍板：行业通用词 + 按钮更窄）
- **触发灵敏度三档**（高 / 中 / 低）：停顿 250/400/800ms、最短草稿（中文 4/8/12 字，英文 2/6/8 字）、夹入英文半词、词后空格、句末标点逐档伸缩；开关旁「三点 + ▾」区域是独立灵敏度触发区（整区点击只开档位菜单、不切换开关），tooltip 跟随档位，选择本地持久化
- 内容自适应：续写语言**跟随草稿**；建议**截断到单句**（到句末标点为止）；**Tab 链式续写**（高档可持续 Tab）
- 质量护栏：角色切换丢弃、退化复读、历史回声（用户前缀 / 助手窗口）、语言一致性；候选全部被过滤时升温 0.5 重试一次，仍无则静默返回空
- 默认关闭并本地持久化；会话主模型非 DeepSeek 时整体隐藏；复用 dsh 里配置的 DeepSeek API key（绝不进浏览器）
- **新会话页可用**：数据面挂 `conversation.input.dock`（dsh shell 在 hero 状态不渲染 composer.dock；input.dock 两种状态都渲染）

