# dsh-archive-session 组合补丁。
# 路线 A 默认开启；backupRoot 是插件自己的备份夹，不动 dsh 会话根目录。
- insert:
    - id: dsh-archive-session
      name: dsh-archive-session
      config:
        backupRoot: !!js dshHomePath('dsh-archive-session-backup')
        titleCacheTtlMs: 60000
        titleCacheMaxEntries: 256
