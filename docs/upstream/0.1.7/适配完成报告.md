# dsh-sparrow × dsh 0.1.7-rc.1 适配完成报告

> 分支 `dsh-0.1.7`｜适配对象：官方 checkout tag `dsh-v0.1.7-rc.1`（HEAD `46a7f68b09`）
> 覆盖：六个活跃插件（theme-tone / archive-manage / codebuddy-credits / chat-fim / file-manage / nav-pin）
> 验证：逐插件 `npm run verify` 全绿（合计 870 条测试）+ 真机 src 模式与 pkg 模式各一轮

---

## 1. 结论

0.1.7-rc.1 把 0.1.5-rc.2 以来（含 0.1.6-alpha.* / 0.1.7-alpha.1~2）的内容一次性合并发布，**六个插件此前全部处于「装得上但半残」状态**（客户端半边整页拖死、宿主半边按格式门自停用、图标改名打崩四个插件的渲染）。本次适配后：

| 插件 | 适配前在 rc.1 上的状态 | 适配后 |
| :--- | :--- | :--- |
| dsh-theme-tone | 客户端 inject 缺 `settingsScope` → **宿主整页起不来** | 全功能，色调/玻璃/设置行在位 |
| dsh-archive-manage | 会话格式门挡 v4 → 宿主自停用；图标名报错 | 全功能（真机侧栏「归档」可用） |
| dsh-chat-fim | 同上 + slot inject 签名 | 全功能（真机输入框 FIM 开关在位） |
| dsh-codebuddy-credits | 宿主面四处契约断裂（settings/图片目标/消息模型）+ 图标 | 全功能（真机模型 pill 正常） |
| dsh-file-manage | 图标 + 端点换代（少一层 `/anthropic`）+ `order` 死参 | 全功能（真机侧栏「云端文件」在位） |
| dsh-nav-pin | 客户端能装，但**核心行为静默失效**（官方换了隐藏目标） | 修复并补注释；窄列观感待真机肉眼确认 |

**未做（按仓库规矩需 owner 当次点头的发布动作）**：CHANGELOG、版本号、tag、npm publish、合并回 `dev`/`main`。本分支停在「适配 + 验证完成」。

---

## 2. 官方变化 → 我方处置（共同面）

| 官方变化（rc.1） | 影响面 | 我方处置 |
| :--- | :--- | :--- |
| 客户端设置基座服务 `settingsScope` → `configForms`；`ConfigForm` 取代 `SettingsScope` | 全部读设置的客户端 | 6 个插件的 client half 迁到 `configForms`；`ctx.inject(['configForms'], …)` 可选 fork |
| 宿主 `settings.register` 移除；插件配置改为**导出 Cordis `Config`**，命名空间 = profile 条目 id | theme-tone、codebuddy | 两插件改为导出 `Config`（字段标 `.volatile()`）；自带界面者在 `ctx.inject(['settings'], …)` 里 `configure({ auto: false })` |
| `SettingsForms.get()` / `installSection()` 移除；`ui-theme` 编辑面改配置模型 | codebuddy | 宿主侧直接读自己的 config 引用，不再经 `SettingsForms` |
| 官方图标改名（`Icon<Glyph><Size>` → `Icon<Glyph><Weight>`，尺寸改 `size` prop）；旧名**全部删除** | 4 个插件的 8 个图标 | 逐处改名 + 显式 `size`，保持观感尺寸 |
| `Context.slots` 声明从退役包 `dsh-client-runtime/client` 迁到 `dsh-client-ui-renderer/client`；`remote`/`sessions`/`modelDirectories` 同理分散 | 全部客户端 | client half 补 type-only 空导入 + devDependency |
| 会话格式 `SESSION_FORMAT_VERSION = 4` | archive、chat-fim 的格式门 | `SUPPORTED_SESSION_FORMAT_VERSIONS` 加 `4` |
| `--dsw-specific-menu` 变半透明玻璃色 + `--dsw-menu-backdrop-filter`（官方样式规则要求成对） | theme-tone | 菜单族 token 改为「只换 RGB、保住官方 alpha」 |
| `[data-sidebar-right-float-host]` 锚点删除 | theme-tone | 移除抬升规则与常量；`ABOVE_FLOAT_HOST_Z_INDEX` → `TAB_MENU_Z_INDEX` |
| `SessionSummary` 新增必填 `agentAvailable` | archive | 按 agents 注册表如实上报 |
| `ImageRequestTarget`：`{maxPixels}` → `{width, height, maxBytes}` | codebuddy | 新增 `requestImageDimensions` 换算 |
| `ContentBlock` 联合删掉 `'tool-result'`；`ToolResultMessage` 形状变化 | codebuddy | 按新消息模型改写 adapter 分支 |
| 官方 DeepSeek 端点 `PUBLIC_BASE_URL` → `https://api.deepseek.com/anthropic`，官方 client 自己补 `/v1` | file-manage | 删硬编码，改读官方导出（最终 `…/anthropic/v1/files`） |
| 官方 Files `list()` 移除 `order` 参数（API 不再提供升序查询） | file-manage | 删掉透传与默认值（死代码） |
| 官方窄列隐藏规则目标 `.slot` → `.frame`（即 `nav` 本身） | nav-pin | 恒显规则改打 `slot > nav` |
| 官方 `@deepseek-ai/dsh-client-runtime` 下线（无 0.1.7 发布） | 全部 | 类型改取 `@deepseek-ai/cordis` / `dsh-session/types`；bundle externals 清死项 |

---

## 3. 逐插件调整明细

### dsh-theme-tone（最重）
- `src/settings-schema.ts`：`ThemeToneSettingsSchema` → 导出 **`Config`**（两个字段 `.volatile()`）。
- `src/host.ts`：`settings.register(...)` 删除 → 导出 `Config` + 可选 `settings` fork 内 `configure({ auto: false })`（自带「设置 → 常规 → 色调」行，避免两个编辑面）。
- `src/client/index.ts`：`settingsScope.bind({namespace})` → `configForms.get('dsh-theme-tone')`；`SettingsScope` → `ConfigForm`；fork 依赖名随之改；注释与文件名引用全部更新。
- `src/constants.ts`：`SETTINGS_NAMESPACE` 从 `'ui-theme-tone'` 改为条目 id（`= name`）；删除 `RIGHT_FLOAT_HOST_ATTR`；层号常量更名 `TAB_MENU_Z_INDEX`。
- `src/backdrop.ts`：删除 `[data-sidebar-right-float-host]` 抬升规则（该锚点已被官方删除）。
- `src/tones.ts`：`POPUP_TOKENS` 形状由 `{token, rung}` 改为 `{token, official:{light,dark}}`；菜单族改走 `washFill`（**保住官方 alpha**）+ `PANEL_TINT`（沿用「浅色轴的面不染色」口径）；`washFill` 增加可选 `scale`。
- 测试：6 条编码旧契约的断言改写为新契约（官方默认逐字回官方、alpha 保持、浅色轴不染色、rung 表只查抬升面）；spec 文档 7 处同步。
- 影响用户的一点：**设置键位从 `ui-theme-tone` 变为 `dsh-theme-tone`，旧选择不自动迁移**（README 已同步为新版本线）。

### dsh-archive-manage
- `src/compat.ts`：格式门 `[0, 3]` → `[0, 3, 4]`（注释同步）。
- `src/client/ArchiveDock.tsx`：`IconArchiveOutline20` / `IconCloseOutline16` → `…Regular`（尺寸由 `size` 传）。
- `src/host.ts`：`emitSessionAdded` 的 `api-session/added` 摘要补 `agentAvailable`。
- 类型面：补 `dsh-client-ui-renderer` / `dsh-api-remotes` / `dsh-api-session-controller` 的 type-only 导入与 devDependency。

### dsh-chat-fim
- `src/compat.ts`：格式门加 `4`。
- `src/client/ChatFimDock.tsx`：`IconSparkle16` / `IconChevronDownOutline14` → `…Regular`。
- `src/client/index.ts`：slot `inject` 工厂签名按官方 `(sessionId: string)` 收口（边界转品牌类型，两版宿主都成立）。
- 类型面：补 ui-renderer / api-session-controller / ui-model-selection。

### dsh-codebuddy-credits
- `src/config.ts`（宿主配置）：改为导出 Cordis `Config`（`apiKeyEnv` / `maxMode` 均 `.volatile()`）；**命名空间 = 自带 `cordis.patch.yml` 的 insert id `llm-codebuddy-credits`**（不是包名——条目 id 由组合层 insert 决定），必须与 `name`、客户端卡片槽位 key、`registerModelDiscovery` key 四处一致，已加守卫测试；形状对齐官方 llm-deepseek 的「整节型 provider」（`apiKeyEnv` 在节根，设置页凭据圆点才会亮）。
- `src/index.ts`：去掉 `installSection` / 节级 `get()` 用法，改读自己的 config 引用（撤销旧间接层）；boot 期「物化 apiKeyEnv + 清 providers」整段删除（rc.1 下 `providers` 的 unset 会抛、且物化不再影响可见状态）；`ImageRequestTarget` 按 `{width, height, maxBytes}` 换算（新增 `requestImageDimensions`，像素预算沿用 2000×2000）；自带设置界面 → `ctx.inject(['settings'], …)` 内 `configure({ auto: false })`。
- `src/adapter.ts`：按 rc.1 的消息模型改写——`ContentBlock` 不再有 `'tool-result'`（工具结果改为一等 `tool` 角色消息，删死分支）；`ToolResultMessage` 直转改为 `role === 'tool'` 分支；顺带按官方口径拒绝新增的 `developer` 角色（旧代码会静默产出空 assistant 消息）。
- `src/client/CodeBuddyModelSelect.tsx`：四个图标改名 + `size`。
- 类型面：补 ui-renderer / api-session-controller / ui-model-selection。

### dsh-file-manage
- `src/host.ts`：删硬编码 `DEFAULT_BASE_URL`，改读官方导出 `PUBLIC_BASE_URL`（能力门加该项，缺失即按既有约定自停用）；`src/files.ts` 新增纯函数 `resolveBaseURL`。
- 新增 `test/base-url.test.mjs`（6 条）：用官方 client + **stub fetch** 钉住真实请求 URL（`https://api.deepseek.com/anthropic/v1/files…`，不发真实请求、不碰凭据）。
- `order` 参数全链路移除（官方 API 已不提供升序查询）。
- 图标改名；spec 文档同步。

### dsh-nav-pin
- `src/nav-pin.ts`：恒显规则由「给外层 `div.slot` 设 `display: block`」改为「给 `slot > nav` 设」，并补注释说明这条前提依赖官方结构（0.1.7 已验证结构变化点）。
- 其余锚点（`[data-conversation-scroll]` / `[data-width-handle]` / `--dsh-*` 宽度变量 / aria-label 文案）核过仍在。

---

## 4. 基础设施与依赖（仓库级）

- `plugins/*/package.json`：官方包依赖 → `0.1.7-rc.1`；cordis → `4.0.4`；schemastery → `^3.18.4`；删 `dsh-client-runtime`；补类型面 devDependencies。
- `pnpm-workspace.yaml`：
  - `overrides` 闭包按 `dsh-v0.1.7-rc.1` 的 manifests **重算**（39 → 62 个包；官方包发布范围仍是 `>=0.1.5 <0.2.0-0`，匹配不到任何预发布版，故仍需逐个 override）。
  - `minimumReleaseAgeExclude` 逐条放行 24h 内的新版本（**pnpm 不支持带版本的 glob**，只能逐条列；发布满 24h 后可整段删）。
- `pnpm-lock.yaml` 重算。
- `scripts/bundle-client.mjs`：清掉已下线的 `dsh-client-runtime/client` 等死 external。
- README（6 插件 × 中英）版本线 → `0.1.7-rc.1`。

---

## 5. 验证证据

**逐插件 verify（typecheck + build + client bundle + node:test + npm pack --dry-run）**

| 插件 | 测试 | 结果 |
| :--- | ---: | :--- |
| dsh-archive-manage | 188 | 全绿 |
| dsh-chat-fim | 202 | 全绿 |
| dsh-codebuddy-credits | 158 | 全绿 |
| dsh-file-manage | 63（含新增 6 条 URL 用例） | 全绿 |
| dsh-nav-pin | 30 | 全绿 |
| dsh-theme-tone | 229 | 全绿 |
| **合计** | **870** | 根目录 `npm run verify` exit 0 |

**真机（隔离 `DSH_HOME` + profile junction，端口 3099/3098，未触碰用户 3080 实例）**

| 检查项 | src 模式（rc.1） | pkg 模式（rc.1） |
| :--- | :--- | :--- |
| 启动告警 `N entry did not activate` | 0 条 | 0 条 |
| 浏览器 console 错误 | 0 | — |
| `[data-slot-error]` 槽位崩溃 | 0 | 0 |
| 插件相关 404 | 0 | 0 |
| theme-tone 样式/背景层 + 暗轴生效 | ✓（body `rgb(10,10,16)`） | ✓ |
| nav-pin 样式注入 | ✓ | ✓ |
| 侧栏入口「归档」「云端文件」 | ✓ | ✓ |
| chat-fim 输入框「FIM」开关 | ✓ | ✓ |
| codebuddy 模型 pill | ✓（DeepSeek-V41-Flash） | ✓ |

---

## 6. 未决 / 未验证（诚实清单）

1. **设置写入未点过**：真机只验证了色调生效与入口在位；「设置 → 常规 → 色调」点选后的**落盘写入**未在真机点过——因为隔离实例的 profile 是 junction 到 owner 真实 profile 的，点选会写进真实配置。组件层写入路径由单测覆盖（`configForms.set/mutate` 契约），如需要可另起一份**拷贝** profile 专门点选验证。
2. **nav-pin 观感待肉眼确认**：窄列（≤900px）恒显与 ≤700px 淡入是行为修复，但真机本轮没有多轮会话可渲染轮次导航，未做视觉确认；建议 owner 在自己实例里拉窄窗口看一眼。
3. **chat-fim 悬浮候选菜单 / 旋转光环**：DOM 锚点静态核对仍在，真机未逐项点过。
4. **`@` 会话列表不过滤归档**（官方行为，非本插件）：host 侧 `session-reference` 不查归档集，只有侧栏搜索跟随「显示归档」视图选项。
5. **archive 与官方 `archive` 入口 id 冲突**：官方已自带归档功能（灰显 + 视图过滤），本插件是增强面板，职责边界待 owner 拍板（讨论稿 02）。
6. **codebuddy 协议兼容性**：仍未发真实请求验证（需 owner 凭据）。
7. **`@deepseek-ai/dsh-client-ui-session` 未进 override 闭包**：导致 session 作用域槽的 `SessionIdOf` 退化为 `string`（当前实现两个世界都成立，不阻塞）；补 override 可让品牌类型生效。
8. **file-manage README 的「最新在前」措辞**：rc.1 下顺序由服务端决定，倾向就是 desc，但未发请求证实，未改。
9. **`docs/spec/13-restore-sidecar-cleanup.md`** 的示例文件名仍是 `session.v3.jsonl.zstd`（rc.1 当前代是 v4）；属历史定案记录，未改。
10. **CHANGELOG / 版本号 / tag / npm publish 未动**——按仓库规矩属发布步骤，需 owner 当次确认。
11. **会话同步读已被官方废弃（rc 线必办，本次未做）**：codebuddy 的 live 账本路径用 `Session.snapshotEvents()`，rc.1 给该方法标了 `@deprecated … new calls are prohibited`；官方替代方向是 Session 投影或显式分页读，属设计级改动（缓存 / 增量折叠 / resume 重做），建议单开任务。现状功能不受影响（rc.1 仍把完整事件序列留在内存）。
12. **codebuddy 图片像素预算语义**：沿用了旧的总像素 2000×2000；官方 CLI 档位是单边 2000px 长边，完全对齐需改用 `longEdgeDimensions`（行为变更），留 owner 决定。
13. **theme-tone 设置行未点选**（同第 1 条）：`configure({ auto: false })` 与 `Config` 投影的运行时表现未在真机点过，静态依据已逐条查证。

---

## 7. 建议的下一步

1. owner 在自己实例（3080，pkg 模式已是 rc.1）刷新页面肉眼过一遍：theme-tone 色调/玻璃、nav-pin 窄列、chat-fim 候选菜单、codebuddy 卡片与 Max 开关。
2. 单开任务：codebuddy 会话读改异步投影（第 11 条）；nav-pin 容器口径变化的真机观感（第 2 条）。
3. 发布流程（需当次点头）：`dev → main` 合并 + 逐插件版本号/CHANGELOG + tag + `npm publish`。

---

## 8. 真机复验后的追加修复（owner 报障 → 修复 → 再验证）

首轮「verify 全绿」不等于真机没问题。owner 在 3080 实测后报了四个现象，逐一定位并修复（全部有真机 A/B 证据）：

| # | owner 现象 | 根因（两版官方契约对照） | 修法 | 真机证据 |
| :--- | :--- | :--- | :--- | :--- |
| 1 | **顶栏崩了** | 会话顶栏槽位在 rc.1 被嵌进 `header[data-slot='conversation.header']`（两层槽位都是 `display:contents`）；旧规则的 `[data-slot='conversation.session.header'] > *` 于是命中官方 `.titleRow`，把它抽成绝对定位浮层 | 改打真正生成盒子的那个 `<header>` | 门一开 header **40px → 10px**，修复后保持 40px、`titleRow` 留在行流 |
| 2 | **菜单全透明**（官方默认模式） | `--dsw-specific-menu` 在 rc.1 从 `var(--dsw-alias-bg-layer-3)`（不透明）变成 `rgba(...,.58)/.5`（半透明玻璃色），官方新增 `--dsw-menu-backdrop-filter` 并规定必须成对使用；我方四个插件都当不透明底色用、从未配 blur | 四个插件的全部消费方（含 sticky 分组标题）补 `backdrop-filter: var(--dsw-menu-backdrop-filter)` | 菜单 `backdropFilter` **none → blur(40px) saturate(1.5)** |
| 3 | **宽度拖拽条没了、两边挤** | `--dsh-chat-content-width` 在 0.1.5 定义在 `.root`(=`[data-phase]`)，rc.1 改到 `.body`(其子元素)；nav-pin 的捕获点写在 `[data-phase]` 上 → 解析为空 → `min(空,…)` 让整条属性 invalid | 捕获点改到「`[data-phase]` 下含滚动体的直接子元素」（两版都成立：rc.1 自带、0.1.5 继承） | 拖拽条 **10px → 0px**，修复后恢复 w=10px、x=406/1144、`cursor:col-resize`；模型标签也不再被截断成「DeepS…」 |
| 4 | 菜单分类标记改坏了 | 同 #2（sticky 分组标题也吃同一个半透明 token） | 同上 | `.ccb-model-groupTitle` 补配对 |

### 第二轮真机报障（owner 继续复验）

第一轮修完后 owner 又报了四条，逐项定位：

| # | owner 现象 | 根因 | 修法 | 证据 |
| :--- | :--- | :--- | :--- | :--- |
| 5 | **鼠标 hover 宽度拖拽条时「光带靠下」，按住才回到鼠标上** | theme-tone 早先为把光带压回顶栏下缘，加过 `[data-width-handle] { top: 76px }`。但官方光带位置是 `var(--dsh-width-handle-pointer-y, 50%)`，`50%` 相对**这个盒子**算 —— 盒子被压成 `[76,720]` 后 `50%=398`，而视口中心是 360，**hover 时（变量从未被写、走兜底 50%）就比屏幕中心低 38px**；拖动时官方写真实 `clientY` 覆盖它，看着才「正常」 | 删掉那条规则，theme-tone 不再碰拖拽条几何（保留 `backdrop.ts` 里只抬层级、不动几何的 `z-index` 那条） | 真机：带规则 `center=398`；移除后 `center=360`（= 视口中心） |
| 6 | **切换色调特别慢，点色卡后半天才换** | `setTone` 只调 `scope.set()` —— 那是跨宿主的一趟往返（写设置文档 → 宿主回镜像 → subscribe 触发 repaint），往返回来前 `readSettings()` 读到的仍是旧值 | **乐观更新**：先把选择记进 `pendingTone` 并立刻 repaint，再发写入；快照追上（或明确偏离 = 被拒）时 `clearSettledPending()` 收掉 | 代码路径 + 单测守卫（顺序：本地落值 → 重绘 → 再发写入） |
| 7 | **右边栏完全没适配** | rc.1 右边栏改由 **dockkit** 承载，真正刷不透明底色的不再是 `.panel`，而是它的**后代**内容宿主 `[data-dockkit-pane]`（= `.tabHost:not(.float), .emptyTabHost { background: var(--dsw-alias-bg-base) }`）；我方图层只画在 `.panel` 上，被那层整个盖掉 | 同一套图层同时画到 `[data-dockkit-pane]` / `[data-dockkit-empty]`（锚点是官方公开 `data-*`） | 真机：`[data-dockkit-pane]` 的 `background-image` 由 `none` → 拿到同源渐变栈 |
| 8 | **6 个色调下背景有竖直分界线** | 面板被抬到 z=82 吃不到 z=80 背景层的光，由我方重画 —— **素材（渐变串）一直对，但合成方式不同构**：背景层深色轴整层 `mix-blend-mode: screen`，颗粒走 `::after` + `opacity` + `screen`（纯加法）；而面板把颗粒当**第 4 个背景层正常合成**（同时压暗），渐变也是正常合成 → 两侧各差一档 | 面板改成与背景层**逐项同构**：3 层渐变 + 独立 `::after` 颗粒（贴图/opacity/混合模式**直引**背景层常量），深色轴补 `background-blend-mode: screen` | 真机 A/B：面板 `::after` 实测 `content:""` / `opacity:0.13` / `mix-blend-mode:screen`（改前是背景层第 4 层、无混合模式）；**逐像素复验（2026-09-24 补）**见下 |
| 9 | **官方弹窗已是透明模糊，我们的色调没跟着** | rc.1 官方菜单族（菜单原语 / 命令面板 / 输入触发器 / 轮次用量 / 子代理血缘）**全部**是 `background: var(--dsw-specific-menu)` + `backdrop-filter: var(--dsw-menu-backdrop-filter)`，两者必须成对；我方兜底规则给它们刷的是**不透明**面板色，把官方模糊盖住了 | 新增 `MENU_MATERIAL_ANCHORS` 追加一条覆盖规则：填充换成**已被色调染过且保留官方 alpha** 的 `--dsw-specific-menu`，并补配套模糊；**不动 `[role='dialog']`**（官方 Modal 本来就是不透明 `--dsw-alias-bg-layer-2`，是另一种材质） | 真机同一菜单元素 A/B：官方档 `rgba(48,49,54,0.5)`；色调档 `rgba(55,53,70,0.5)`，两档 `backdrop-filter` 都是 `blur(40px) saturate(1.5)` |

### 第三轮真机报障（owner 继续复验，含**三条官方自身 bug**）

owner 在**同事机器的干净官方安装**上复现了其中两条，确认非本插件所致：

| # | owner 现象 | 根因 | 修法 | 证据 |
| :--- | :--- | :--- | :--- | :--- |
| 10 | **hover 拖拽条时光带靠下，按住才回到鼠标**（owner 在干净官方上复现 ⇒ **官方 bug**） | 官方 `--dsh-width-handle-pointer-y` **只在拖拽中**被写（`onPointerMove` 首行 `if (!dragging.current) return`），悬停只能用 CSS 兜底 `50%`；而 `50%` 相对拖拽条盒子算 → 盒子 `[76,720]`、`50%=398`，视口中心 360 | **nav-pin** 补写官方自己那个变量：悬停时用**官方同一公式**（`clientY - box.top`）写它，拖拽期间让位给官方 | 真机 + 6 条纯函数守卫。**曾先试纯 CSS 改兜底值**，因两处硬伤放弃（修不掉「点住才回鼠标」；且 38px 偏移只在顶栏于流内时存在，theme-tone 浮层化后反而偏高） |
| 11 | **浅色模式选色调无法维持，很快回到深色官方黑** | `setTone` 写入时**重新查询** `ctx.theme.getTheme()` 决定写哪个字段，而行渲染的轴来自 store 的 `colorScheme` —— 模式切换的时间窗内两者错开，把**浅色 id 写进深色字段**；而两轴合法集合**不重叠**（浅 official/blue/sakura/green、深 official/violet/crimson/forest）→ 宿主 schema 拒绝 | 写轴**由行传入**（= 渲染轴）：`setTone(id, scheme)` | 代码路径 + 新回归守卫（断言 `setTone` 段内不得再查 `getTheme()`） |
| 12 | **弹层材质不统一**：有的全透明、有的不透明、有的有颗粒有的没 | 后台任务列表那个 `<ul>` 是全部弹层里**唯一没有 `role`** 的，只进了 `SURFACE_ANCHORS`（不透明 + 图层）却**漏进** `MENU_MATERIAL_ANCHORS`（半透明 + 官方模糊）→ 只有它不透明且无模糊 | 把它收进材质表（官方那 `<ul>` 自己就是 `--dsw-specific-menu` + `--dsw-menu-backdrop-filter`） | 生成的 CSS 实测：该 `ul` 现在两条规则齐备 |
| 13 | **官方模型选择菜单：分类显示条有背景色且与后面串色**（**官方 bug**） | 脚本逐文件审计官方 rc.1（扫「`background: var(--dsw-specific-menu)` 后 6 行内有无 `backdrop-filter`」）→ **弹层表面只剩一处漏配**：`ModelSelect.module.css:175` 的 `.groupTitle`（`position: sticky`）。0.1.7 把该 token 改成半透明，却漏给这条 sticky 标题配模糊（同文件 `:108` 的菜单面就配了）→ 列表从它底下滚过去时透出来 | 补 `backdrop-filter: var(--dsw-menu-backdrop-filter)`，**不动底色** | 生成的 CSS 实测该规则就位 |

### ⚠️ 最要紧的一条：官方**孤儿写锁**会让「所有设置修改静默失效」

owner 报「刚才这些问题一个都没修好，我没重启 dsh，是否需要重启？」——**跟插件与重启都无关**，
是官方原子写锁留下的一个**孤儿锁**把设置写入整条通道堵死了：

```
POST /api/settings/mutate  →  200
{"ok":false,"error":{"code":"settings/rejected",
 "message":"atomic-write: timed out waiting for the writer lock at
            C:\\Users\\DJ028191\\.dsh\\profiles\\web\\package.json.lock"}}
```

现场证据：锁文件内容 = PID **15852**，该进程**已不存在**；锁写入时间 **15:19:59**，
而 dsh 进程启动于 **15:23:48**（比锁晚 3 分 49 秒）——即一个**前一次进程异常退出的遗留死锁**。

官方源码对这个设计有明确说明（`packages/util/atomic-write/src/index.ts`）：

> *The contender never removes an existing lock because file age cannot prove that its owner
> stopped; **orphan recovery is an operator action**.*

也就是说官方**有意不自动回收**，只能人工删。它的表现极具迷惑性：
前端先乐观显示新值 → 写盘被拒 → 官方 `adopt()` 拿回权威值 → 界面**几秒后弹回旧值**，
于是「任何插件改动看起来都没生效」。

**owner 的关键补充**帮助纠偏了我的初判：「深色的三个色调没有这个问题，只有浅色有问题」——
实测后明白：深色那三个是**早就保存过**的值（配置里已有），显示正常；浅色每次都是**新写入**，
每次都撞锁。**不是浅色轴有 bug，而是「任何新写入」都坏了**，浅色只是恰好总需要新写入。

清除孤儿锁后的对照实测（同一实例、同一页面）：

| 项 | 清锁前 | 清锁后 |
| :--- | :--- | :--- |
| 切浅色 | 2.1s 后**弹回 dark** | **4.2s+ 稳定 LIGHT** |
| `settings/mutate` 返回 | `ok:false`（锁超时） | `ok:true` |
| 深色选 violet | — | 落盘 `darkTone: violet`、mtime 更新 |
| 浅色选 sakura | — | 保持选中、`gate: TONE`、`bgBase: #fff` |

**排查手法（可复用）**：怀疑「设置改了不生效」时，先在页面里挂 `fetch` 钩子抓
`/api/settings/mutate` 的**响应体**——`ok:false` 且带 `settings/rejected` 就是写盘被拒，
再看错误里那个 `.lock` 路径与 PID 是否已成为死进程。这比反复刷新页面有效得多。

### 「改了插件代码，需要重启 dsh 吗？」——**不需要，刷新即可**（已核实）

owner 问过两次，用证据定案（`link:` 装载的插件）：

* 服务端按**内容派生的 `rev`** 拼 bundle URL，如
  `/plugins/??@dsh-sparrow/dsh-nav-pin/client.js&rev=399479857a18`；
  文件一变 `rev` 就变，而 `?rev=` 变化即绕过 `immutable` 缓存。
* **实测**：在**普通刷新**（非强制）的页面上抓那两个 bundle 的响应体，核到我这一轮的标记 ——
  nav-pin 含 `handle glow follows pointer` / `shouldDriveGlow`；
  theme-tone 含 `syncRow` / `setTone` / `dsw-menu-backdrop-filter`。
* **行为侧**同样实测：普通刷新的页面上派发一次 `pointermove`，光带变量
  `(未设置) → 50px`（= `clientY - box.top`），说明新监听确实在跑。

所以流程是：**改代码 → `npm run verify` → 刷新页面**（浏览器若仍跑旧 bundle，强刷一次）。
只有**新增/移除插件条目**（`package.json` 的 bundles 变化）才需要重启，改代码不需要。

**官方档「零改动」复验**（owner 新约束：「官方的深浅两个主题，我们应该完全不动才对吧」）——
把我方 38 条 token 覆盖 × 明暗两轴逐条比对官方 `design-platform.css`：

| 结果 | 条数 |
| :--- | ---: |
| 与官方值**完全相同** | 64 |
| 我方自有变量（`--dsh-theme-tone-*`，官方没有） | 12 |
| **与官方不同** | **0** |

叠加门属性关闭（所有 CSS 规则失效）+ 背景层 `hidden` —— **官方档三层零干预**成立。

> ⚠️ **一处有意的例外**：官方那处**漏配的 sticky 分组标题模糊**（#13）是**不带门**的 ——
> 它修的是**官方自己的 bug**，在官方档下也必须生效（owner 的报障截图正是官方档）。
> 与悬停卡同一条口径：只补官方**本来就想要**的那个属性（它其余菜单面全配了模糊），
> 不引入任何外来色相。所以「零改动」的口径精确化为：
> **不改变官方任何外观决策；官方自己写错/漏写的地方照修。**

### ①「多色调下背景竖直分界线」的逐像素复验（2026-09-24）

在真机（3080、深色 violet 色调、右栏面板打开）上截整屏，逐列取**中位 R 值**
（中位数天然滤掉正文文字的干扰），重点看面板左缘 `x=704` 两侧：

| 采样列 | 现在（我方图层生效） | 临时关掉我方 `background-image` + `::after` |
| :--- | ---: | ---: |
| x=703（会话区侧） | 23 | 23 |
| x=705（面板侧） | **22** | **20** |
| x=712 / 760 | 22 / 22 | 20 / 20 |
| x=1200（面板深处） | 22 | 19 |

**跨缘差值：现在 1 档 vs 关掉后 3 档** —— 面板侧亮度被我的图层抬到与会话区一致，
分界线随之消失。这是这一条从「方向正确」到「逐像素可验」的补齐。

**默认首页复核**（owner 先前报的「两个红框位置」）：真机实测几何正常 ——宽度轴解析出 `clamp(680px, calc(1000px*.64), 920px)`（那个「解析为空」的 bug 已随 #3 修掉）；
输入卡 x=421 w=712（中心与会话区中心差 3px）；CodeBuddy 入口右距 28 / top 14（= 官方 header 内边距）；
hero 相位拖拽条 0 条（官方本来只在 active 渲染）。

### 全面自查（owner 要求）产出的额外发现

- **拉网式 token 对照**：把我方引用的 89 个 `--dsw-*` 逐个在两版官方样式里取值比对 —— **只有两个** token 的语义变了（`--dsw-specific-menu`、`--dsw-menu-backdrop-filter`），正是 #2。其余 87 个取值一致。
- **DOM 锚点全量核验**：我方依赖的 16 个官方 `data-*` 锚点在 rc.1 全部存在（`data-width-handle`、`data-composer-card/seat`、`data-phase`、`data-trigger-menu`、`[data-slot=…]` 等）；唯一消失的是 `[data-sidebar-right-float-host]`（首轮已处理）。
- **静态色阶全量核验**：RUNG 表引用的 19 个 `--dsw-static-*` 在 rc.1 全部存在。
- **修掉一处历史错名**：`--dsw-alias-state-warning-primary` 在**两版官方里都不存在**（真名是 `-warn-`），我方 7 处引用它，只因都带 `#d9822b` 兜底而静默走 fallback —— 已全部改正（archive 6 处、chat-fim 1 处）。与 0.1.7 无因果关系，是自查顺带修掉的。

### 教训（已写进代码注释与回归守卫）

**「verify 全绿」只覆盖类型/单测，覆盖不了「官方契约在两版之间悄悄换了语义或 DOM 结构」。** 这四类问题的共性是：我方规则**语法正确、类型正确、单测也过**，只是假设过时。故每条都补了带「为什么」的注释 + 一条回归守卫测试，钉住新的契约形状（例如「顶栏不得再打会话槽位的直接子元素」）。

---

## 9. 提交记录（分支 `dsh-0.1.7`）

| commit | 内容 |
| :--- | :--- |
| `cfcbece` | chore: 依赖升线到 dsh 0.1.7-rc.1（六插件 + workspace overrides 闭包重算） |
| `c684222` | fix: theme-tone 适配（host Config + 客户端 configForms + 菜单玻璃） |
| `223381a` | fix: archive-manage 适配（会话格式 v4 + 图标改名 + agentAvailable） |
| `d2f4be4` | fix: chat-fim 适配（会话格式 v4 + 图标改名 + slot inject 签名） |
| `c494020` | fix: file-manage 适配（端点换代 + order 移除 + 图标改名） |
| `0d29617` | fix: nav-pin 适配（官方隐藏目标换成 nav 本身） |
| `7a133eb` | fix: codebuddy-credits 适配（宿主面改 Config + 图片目标 + 消息模型） |
| `bf5d14b` | docs: 0.1.7-rc.1 适配完成报告 |
| `9ce37c4` | fix: nav-pin 宽度轴在 0.1.7 上失效（捕获点写在了错误的元素上） |
| `a399219` | fix: theme-tone 顶栏在 0.1.7 上崩掉 + 半透明菜单面缺 backdrop-filter |
| `aa91926` | fix: 告警色 token 名写错（`--dsw-alias-state-warning-primary` 从来不存在） |

另有两个更早的讨论稿提交（`2838d39` 主题立场、`aba8e53` 归档语义）。
